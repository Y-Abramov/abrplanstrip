using Abr.Civil.Sdk;

namespace AbrCivil.PlanStrip
{
    internal sealed class AboutDialog : AbrAboutForm
    {
        public AboutDialog() : base(
            "Развёртка плана",
            "Спрямление ситуационного плана трассы для полосы над сеткой продольного профиля.\r\n\r\n"
            + "Полоса собирается из объектов чертежа и внешних ссылок, попавших в заданный коридор "
            + "вдоль трассы, привязывается к виду профиля и пересчитывается командой «Обновить».")
        {
        }
    }
}
